1. Sintaxe básica
→ console.log()
→ comentários
→ ponto e vírgula
→ blocos {}

2. Variáveis
→ let
→ const
→ var

3. Tipos de dados
→ String
→ Number
→ Boolean
→ Array
→ Object
→ null / undefined

4. Operadores
→ + - * / %
→ == ===
→ != !==
→ > < >= <=
→ && || !

5. Condicionais
→ if
→ else
→ else if
→ switch

6. Repetições
→ for
→ while
→ do while
→ for...of
→ for...in

7. Funções
→ funções tradicionais
→ parâmetros
→ return
→ arrow functions

8. Arrays e objetos
→ métodos de array
→ propriedades
→ métodos
→ destructuring

9. DOM
→ document.querySelector()
→ textContent
→ classList
→ eventos
→ formulários

10. JavaScript moderno
→ let/const
→ template literals
→ spread/rest
→ modules
→ promises
→ async/await

11. APIs
→ HTTP
→ JSON
→ fetch()
→ consumo de APIs
